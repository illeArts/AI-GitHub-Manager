# Release automation

`release-v1.6.1-fixed.yml` is the canonical workflow for AI GitHub Manager v1.6.1.

The earlier `release-v1.6.1.yml` file is superseded and must not be used for future versions.
